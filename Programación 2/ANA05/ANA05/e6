import numpy as np

# Crear una matriz de 2x4
matriz = np.array([[1, 2, 3, 4],
                   [5, 6, 7, 8]])

# Acceder a una porción de la fila 0 (primera fila), por ejemplo, desde la columna 1 (segunda columna) hasta la columna 2 (tercera columna)
porcion_fila = matriz[0, 1:3]

# Mostrar la porción de la fila en pantalla
print("Porción de la fila 0, columnas 1 a 2:")
print(porcion_fila)
